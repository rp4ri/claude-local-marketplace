---
description: "Technology migration workflow — inventory, compatibility, phased plan, execution with rollback points."
argument-hint: "<from> to <to> [--plan-only | --execute | --dry-run]"
allowed-tools: ["Read", "Write", "Edit", "Bash", "Grep", "Glob", "Agent", "WebSearch"]
---

Run the migration workflow. Read the skill at ${CLAUDE_PLUGIN_ROOT}/skills/migrate/SKILL.md and execute it with the provided arguments.
