---
description: "Structured 4-phase code review — context, architecture, line-by-line, verdict. Works on staged changes, PRs, or paths."
argument-hint: "[path | --staged | --pr current | --pr 123]"
allowed-tools: ["Read", "Bash", "Grep", "Glob", "Agent"]
---

Run the review process. Read the skill at ${CLAUDE_PLUGIN_ROOT}/skills/review/SKILL.md and execute it with the provided arguments.
