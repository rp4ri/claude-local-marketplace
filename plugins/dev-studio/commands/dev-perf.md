---
description: "Runtime performance analysis — blocking I/O, sequential awaits, memory leaks, bundle size, load waterfall."
argument-hint: "[path | --routes | --server | --client | --bundle]"
allowed-tools: ["Read", "Bash", "Grep", "Glob", "Agent"]
---

Run the performance analysis. Read the skill at ${CLAUDE_PLUGIN_ROOT}/skills/perf/SKILL.md and execute it with the provided arguments.
