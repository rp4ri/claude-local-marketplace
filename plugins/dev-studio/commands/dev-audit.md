---
description: "Unified codebase audit — N+1 queries, dead code, blocking async, security smells. One command, actionable output."
argument-hint: "[path | --staged | --fix | --quick]"
allowed-tools: ["Read", "Write", "Edit", "Bash", "Grep", "Glob", "Agent"]
---

Run the audit pipeline. Read the skill at ${CLAUDE_PLUGIN_ROOT}/skills/audit/SKILL.md and execute it with the provided arguments.
